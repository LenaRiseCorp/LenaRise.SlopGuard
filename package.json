{
  "name": "lenarise-slopguard",
  "version": "0.6.4",
  "private": true,
  "license": "Apache-2.0",
  "description": "Agentic quality guard: blocks slop patterns at the hook layer.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "docs": "node scripts/gen-docs.mjs",
    "selfscan": "node scripts/selfscan.mjs",
    "verify": "npm test && npm run selfscan && npm run docs -- --check",
    "mutate": "node scripts/mutate.mjs"
  },
  "dependencies": {},
  "devDependencies": {},
  "repository": {
    "type": "git",
    "url": "https://github.com/LenaRiseCorp/LenaRise.SlopGuard.git"
  }
}
